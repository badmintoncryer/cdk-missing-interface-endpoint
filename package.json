{
  "name": "cdk-missing-interface-endpoint",
  "version": "1.0.0",
  "description": "AWS CDKで未対応なInterface Endpoint一覧作成ツール",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.5.1"
  },
  "dependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
